# The edge function will run for every request,
# since "/*" matches every URL path.
[[edge_functions]]
function = "changeDom"
path = "/home"

[[redirects]]
  from = "/home"
  to = "https://www.testhq.com/"
  status = 200
  force = true
  headers = {X-From = "Netlify"}
